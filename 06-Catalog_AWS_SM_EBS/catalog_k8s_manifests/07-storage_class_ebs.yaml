# ebs volume is created only when a pod is scheduled
# ebs volume is created in the same az as the worker node

apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata: 
  name: ebs-sc
provisioner: ebs.csi.aws.com
volumeBindingMode: WaitForFirstConsumer 

